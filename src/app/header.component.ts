import { Component } from '@angular/core';
@Component({ selector: 'app-header', standalone: true, template: `<header class="nav"><button class="brand" (click)="scrollTo('top')" aria-label="Mcube Architects and Consultants home"><span>MCUBE</span><i></i><span>ARCHITECTS &amp; CONSULTANTS</span></button><button class="menu-toggle" (click)="menuOpen = !menuOpen">{{ menuOpen ? 'Close' : 'Menu' }}</button><nav [class.open]="menuOpen"><button (click)="scrollTo('work')">Work</button><button (click)="scrollTo('studio')">Studio</button><button (click)="scrollTo('contact')">Contact</button></nav></header>`, styleUrl: './app.component.scss' })
export class HeaderComponent { menuOpen = false; scrollTo(id: string) { document.getElementById(id)?.scrollIntoView({ behavior: 'smooth' }); this.menuOpen = false; } }
